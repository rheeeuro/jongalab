-- 2026-08-05: 재료 지속성 실험실 룰 2종 (candidate) — 축은 **익일 아침 매수 여부**
--
-- [초점] 사용자 정의: "재료의 지속성 = 내일 갭상승하려면 **오늘 재료가 내일도 지속적으로
-- 매수할만한가**". 그래서 두 rule 의 `exit_label` 은 `exec_leg_ret`(종가 진입 → 익일 아침 청산)
-- 이다 — 이게 곧 갭 실현치다. `mat_run_ret_3d`(D+3, sql/53)는 "재료가 실제로 계속 먹혔나"를
-- 되짚는 **보조 정답지**이고 승격 판정의 자가 아니다.
--
-- [왜 2종만인가 — 다중검정 비용]
-- 이미 candidate 가 35종이다(selector 23·veto 8·benchmark 4). 이 프로젝트 자체 시뮬레이션이
-- "후보 24종이면 기대 오탐 5.2건"이라 결론냈으므로(edge_policy DISCOVERY_DAYS 주석) 후보를
-- 늘리는 건 공짜가 아니다. 그래서 **기존 룰로 이미 측정되는 것은 등록하지 않는다**:
--   · 소진 배제는 `veto_news_spent`(소진 전체, 실측 9~13건/일)가 이미 빠르게 재고 있다.
--     '소진 & 과열' 같은 좁은 변형은 그 결과를 본 뒤에 만든다.
--   · '연속 & 산업사이클'은 별도 등록하지 않는다 — 아래 imminent 와 `f1_news_durable`(합집합)의
--     **뺄셈으로 분리된다**. 룰 1개로 두 경로를 다 본다.
--   · 재료 규모(`news_material_size_ratio`)는 금액 추출률이 3/22(14%)라 표본이 너무 느리다 → 보류.
--
-- [왜 이 두 개인가 — '연속' 안에 서로 다른 집단이 섞여 있다]
-- 실측 2026-08-05 판정 22종목: '연속' 7건 중 **6건이 산업사이클 경로**이고 촉매 임박(1개월 내)
-- 경로는 1~2건이었다. 즉 `f1_news_durable`(연속 전체)은 사실상 "산업사이클 룰"로 동작하며
-- v2 가 새로 넣은 임박 축을 가린다. 두 경로는 매수자가 다르다:
--   · 임박  — "곧 결정 난다"를 보고 내일 아침 사는 사람
--   · 사이클 — 동종주 확산·후속 보도를 보고 내일 아침 사는 사람
-- 섞어 재면 어느 쪽이 갭을 만드는지 알 수 없다.
--
-- ⚠️ **현재 표본에서 '연속'의 갭 근거는 약하다**(정직한 기록): 익일 시가 같은 날 초과
-- +0.77%p 지만 t=0.51 이고, 뚜렷했던 건 오히려 익일 **장중**(양수율 84%, n=19)이었다. 사용자
-- 정의대로 갭으로 판정하면 이 룰들은 떨어질 수 있다 — 그게 발견창의 목적이다.
--
-- [표본 속도] imminent ~2건/일, unpriced ~2건/일(8/5 실측). 게이트 문턱은 종목-일 n 이 아니라
-- **거래일 수**(PROMO_MIN_DAYS=10)이므로 좁은 룰도 판정은 가능하다(min_sample 은 참고값이라
-- 40 대신 20 으로 현실화). 다만 하루 1~2건이면 `ci_low>0` 을 통과하기 어렵다는 점은 감안한다.
--
-- [선행 조건 — 이 룰들이 측정 가능해진 이유 2가지]
--   ① `SELECTION_TIME_COLS` 누락 픽스(2026-08-05): `news_durability` 가 그 집합에 없어서
--      기존 지속성 rule 2종이 7/29 등록 이후 `rule_layer=None`(실행 불가) 판정을 받고 있었다 —
--      통계 게이트를 통과해도 실행 가능성 게이트에서 영구히 막히는 상태였다.
--   ② 리드문 우선 선별(select_headlines): `news_milestone_horizon` 은 제목에 안 실려 리드문이
--      유일한 근거인데, 리드문이 8종목분만 있던 실행에서 horizon 이 17/17 전부 '불명' 이었다.
--      리드문이 붙은 코퍼스로는 불명 14·그이후 6·1개월내 2 로 축이 살아난다.
--      → 이 픽스 전에는 imminent 가 **영원히 매칭 0** 이었을 것이다.

INSERT IGNORE INTO edge_rule
  (name, title, family, role, description, predicate, exit_label, status, min_sample, registered_at)
VALUES
('f1_material_imminent', '촉매가 임박한 재료', 'f1_news', 'selector',
 '재료에 남은 다음 일정이 **한 달 안에** 있는 종목만 고르는 규칙입니다. 종가베팅은 오늘 종가에 사서 다음 날 아침에 파는 전략이라, 다음 날 아침에도 이 종목을 살 사람이 있어야 갭상승이 나옵니다. "허가 결정이 이달 말", "다음 달 본계약" 처럼 결과가 곧 나오는 재료는 다음 날 아침에도 기대를 사는 사람이 있지만, "내년 양산", "장기 과제" 처럼 먼 이야기는 오늘 뉴스를 본 사람도 굳이 내일 아침에 서둘러 살 이유가 없다는 아이디어입니다. 기존 "이어지는 재료" 규칙은 임박한 재료와 산업 사이클 재료를 함께 담는데, 실측에서 그 대부분이 산업 사이클이라 임박 여부의 효과가 가려져 있었습니다. 그래서 임박한 재료만 따로 떼어 성적을 기록합니다. 실제 매매에는 아직 적용하지 않습니다.',
 '[{"col":"news_durability","op":"==","value":"연속"},{"col":"news_milestone_horizon","op":"in","value":["1주내","1개월내"]}]',
 'exec_leg_ret', 'candidate', 20, CURDATE()),

('f1_material_unpriced', '살아있는 재료, 아직 안 오름', 'f1_news', 'selector',
 '재료가 아직 끝나지 않았는데 당일 주가는 0~5%만 오른 종목을 고르는 규칙입니다. 다음 날 아침에 살 사람이 있으려면 두 가지가 같이 필요합니다 — 재료가 내일도 유효해야 하고(이어지는 재료), 오늘 이미 다 오르지 않았어야 합니다. 오늘 15% 급등한 종목은 재료가 살아있어도 다음 날 아침 매수자에게는 이미 비싼 가격이라, 재료의 지속성이 갭상승으로 이어지지 않는다는 아이디어입니다. 뉴스 건수를 보는 "신선한 재료 미반영" 규칙과 축이 다릅니다 — 그쪽은 기사가 처음 떴는지를 보고, 이 규칙은 AI가 읽은 재료의 성질이 이어지는지를 봅니다. 실제 매매에는 아직 적용하지 않고 성적만 기록합니다.',
 '[{"col":"news_durability","op":"==","value":"연속"},{"col":"change_pct","op":"between","value":[0,5]}]',
 'exec_leg_ret', 'candidate', 20, CURDATE());
