-- 2026-08-05: 지속성 rule 2종을 v2 표본으로 재시작 (registered_at 리셋 + 설명 갱신)
--
-- [왜 리셋인가]
-- `news_durability` 의 **정의가 바뀌었다**(sql/52, 합성 규칙 v2):
--   · '연속' 조건에서 `amount_locked=0` 을 빼고 `milestone_horizon`(촉매 1개월 내)을 넣었다
--   · stage='불명' 을 더 이상 판정 불가로 막지 않는다(커버리지 회복)
--   · 텍스트 코퍼스에 네이버가 들어와 라벨 대상이 유니버스의 30% → 95% 로 넓어졌다
-- 정의가 다른 라벨을 한 표본으로 채점하면, v1 표본으로 쌓은 통계로 v2 룰을 승격시키는 셈이
-- 된다. 그래서 발견창(DISCOVERY_DAYS)을 오늘부터 다시 시작한다.
-- ⚠️ 과거 채점 이력(edge_rule_daily)은 **여기서는 남겼다가 sql/57 에서 삭제했다** — 게이트는
-- registered_at 이후만 보므로 숫자는 정확했지만, 화면에 "누적 성적은 비어 있는데 매칭 기록에는
-- 7/29~8/4 가 보이는" 상태가 되어 혼란을 샀다(사용자 지적). 그 5거래일치 수치는 sql/57 주석에
-- 남겼다. 앞으로 라벨 정의를 바꿔 리셋할 때는 **매칭 기록까지 같은 마이그레이션에서 정리**한다.
--
-- [status 는 candidate 유지]
-- 실측 방향은 정합하지만(마무리 -1.66%p t=-2.79 / 연속 익일 장중 양수율 84%) 4거래일 표본이라
-- PROMO_MIN_DAYS=10 에 못 미친다. 승격 판정은 표본이 찬 뒤 게이트 재계산으로 한다
-- (veto 는 대체효과 + exit_label 부호 교차확인, selector 는 일 클러스터 t + 대조군 초과).

UPDATE edge_rule
SET registered_at = CURDATE(),
    stats = NULL,
    decision = NULL,
    description = '뉴스 재료가 이미 다 소진된 종목을 매수 대상에서 빼면 이득인지 확인하는 관찰용 규칙입니다. "소진"은 AI가 헤드라인과 기사 첫 문단을 읽고 ① 재료가 마무리 국면이거나 ② 재료에 남은 다음 일정이 아예 없다고 판정한 경우입니다. 예를 들어 "2분기 영업이익 3,000억 확정 발표"나 "지분 매각 완료"는 소진이고, "FDA 실사 종결(허가 결정이 남음)"은 소진이 아닙니다. 종가베팅은 오늘 종가에 사서 다음 날 아침에 파는 전략이라 다음 날 아침에도 살 사람이 있어야 하는데, 재료가 오늘 끝났으면 그 사람이 없다는 아이디어입니다. 실측에서 마무리 국면 종목은 다음 날 아침 같은 날 다른 종목보다 1.66%p 낮았습니다(표본 22건). 실제 매매에는 아직 적용하지 않고 제외했을 경우의 성적만 매일 기록합니다.'
WHERE name = 'veto_news_spent';

UPDATE edge_rule
SET registered_at = CURDATE(),
    stats = NULL,
    decision = NULL,
    description = '뉴스 재료가 한 번으로 끝나지 않고 주가를 이어서 올려줄 성질인 종목을 고르는 규칙입니다. AI가 헤드라인과 기사 첫 문단을 읽고 ① 재료에 다음 예정 사건이 남아 있고(허가 결정, 본계약, 예산 집행, 다음 임상 단계 등) ② 그 사건이 한 달 안에 오거나 정책·산업 사이클처럼 동종업계가 함께 움직이는 재료일 때 "연속"으로 판정합니다. 사건이 남았어도 내년처럼 먼 이야기면 "연속"이 아닙니다 — 하루만 들고 있는 전략에서는 임박한 일정만 다음 날 매수세를 만듭니다. 참고 실측(표본 19건이라 근거는 아님): 연속으로 판정된 종목은 다음 날 장중에 84%가 올랐습니다(소진 46%, 뉴스 없는 종목 61%).'
WHERE name = 'f1_news_durable';
