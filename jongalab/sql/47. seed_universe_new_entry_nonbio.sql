-- 거래대금 상위 첫 등장 — 바이오 제외 변형 (2026-08-04, 사용자 결정)
--
-- 배경: `f5_universe_new_entry`(first_seen=1 + 등락률 3~15%)는 2026-07-29 발견 판정에서
-- 탈락했고(그 시점 평균 -0.405%), 이후 표본이 쌓여 누적 평균은 +0.182% 로 양수가 됐지만
-- **보수적 수익(ci_low) -1.423%** 로 승격 게이트를 통과하지 못한다. 원인은 평균이 아니라
-- **변동성**이다(std 6.34, 승률 0.50, 최악 저가 -29.885% = HLB 2026-07-09 하한가).
-- 아이디어는 살리고 꼬리를 줄여 보려는 변형이 이 rule 이다.
--
-- ⚠️ **효과는 측정되지 않았다 — 통설 기반 가설이다.** 등록 전 인샘플 점검에서 확인한 사실:
--   · `is_bio` 피처는 2026-07-10(sql/16) 도입이라 **HLB 7/9 행의 is_bio 는 NULL** 이다.
--     즉 과거 표본에 `is_bio=0` 을 걸면 HLB 가 빠지지만 그건 "바이오라서"가 아니라
--     "피처가 없던 날짜라서"다(NULL=매칭 실패). 개선처럼 보이는 수치(ci_low -1.42 → -0.43)의
--     대부분이 이 아티팩트다.
--   · 피처가 채워진 구간만 잘라 정직하게 비교하면 효과가 거의 없다:
--       전체 n=33 평균 +0.653% std 3.95 ci_low -0.475 최악저가 -18.95
--       바이오 제외 n=29 평균 +0.844% std 4.18 ci_low -0.430 최악저가 -17.63
--     평균은 +0.19%p 올라가지만 **std 는 오히려 커지고 최악 꼬리는 거의 그대로**다.
--   · 실제 매칭된 바이오 4건은 현대약품·온코닉테라퓨틱스·삼성바이오로직스·셀트리온으로
--     전부 -0.2~-0.9% 소폭 손실이고 **꼬리를 만든 종목이 아니다**. 반대로 HLB 7/13~15 행
--     (is_bio=1, +0.8/+1.6/+11.0%)은 이 조건이 버리는 수익이다.
-- → 그래서 근거는 **"바이오는 임상·허가 결과에 따라 하루에 상·하한가로 튀는 바이너리 리스크가
--   있다"는 통설**뿐이다. 앞으로 수집되는 표본에서는 HLB 류가 is_bio=1 로 정상 채워지므로
--   (7/13~15 행으로 확인) 조건이 의도대로 그 종목군을 걸러낸다 — 그 전제에서만 검증 가치가 있다.
--   candidate 로만 등록하고, 사전 등록 원칙대로 **오늘(CURDATE) 이후 표본으로만** 판정한다.
--
-- 설계 메모:
--   · 원본 rule 은 그대로 둔다(판정 종결 상태로 채점만 계속 — 두 성적을 나란히 비교하는 것이
--     이 변형의 유일한 판정 근거다). 원본을 고치면 사전 등록 표본이 오염된다.
--   · `is_bio` 는 SELECTION_TIME_COLS 라 승격 시 선정 시점(13~15시·19:30 회차) 실행 가능.
--   · is_bio NULL(업종 조회 실패 등)은 매칭 실패로 자연 제외 — 보수적 방향이다.
--   · family/role/exit_label 은 원본과 동일하게 맞춰 같은 자로 비교한다.

INSERT IGNORE INTO edge_rule
  (name, title, family, role, description, predicate, exit_label, status, min_sample, registered_at)
VALUES
('f5_universe_new_entry_nonbio', '거래대금 상위 첫 등장(제약·바이오 제외)', 'f5_supply', 'selector',
 '최근 2주간 거래대금 상위권에 없다가 처음 등장한 종목 중 제약·바이오를 뺀 것만 고른다. 원래 전략("첫 등장")은 평균 수익은 플러스인데 성적의 출렁임이 너무 커서 적용 기준을 넘지 못했다 — 하루에 -30%까지 빠진 종목이 섞여 있었기 때문이다. 제약·바이오는 임상 결과나 허가 발표 하나로 하루에 상한가·하한가까지 튀는 성질이 있어, 그 종목군을 빼면 출렁임이 줄어들 것이라는 아이디어다. 다만 이건 아직 데이터로 확인된 것이 아니라 통념에 기댄 가설이고, 과거 성적으로는 개선 효과가 거의 없었다. 그래서 실매매가 아닌 관찰 상태로 등록해 앞으로의 성적을 원래 전략과 나란히 비교한다.',
 '[{"col":"first_seen","op":"==","value":1},{"col":"change_pct","op":"between","value":[3,15]},{"col":"is_bio","op":"==","value":0}]',
 'exec_leg_ret', 'candidate', 40, CURDATE());
