-- 2026-08-05: 악재 뉴스 제외 완화 문턱 candidate 1종 (`veto_bad_news` 는 무변경)
--
-- [문제] `veto_bad_news`(live, 2026-07-07 등록)가 **20거래일 연속 매칭 0건**이다.
-- `edge_rule_daily` 20행 전부 n_matched=0, stats 는 n:0 — 자금 경로에 얹혀 있으면서
-- 한 번도 발동한 적이 없고, 발동하지 않으니 강등 감시(제외 종목 mean_net>0)도 영원히 침묵한다.
-- 원인은 세 겹이고, 세 번째가 결정적이다.
--
--   ① 라벨 커버리지 (해결됨) — 7/28 까지는 Ollama 요약 후보 top-5 만 라벨링해서 유니버스
--      33~51행 중 라벨이 0~5행이었다(7/20·7/28 은 0행). 7/29 OpenAI 벌크로 23~36행,
--      8/5 코퍼스 기준 전환으로 32/63 행. 채점할 모수가 이제야 생겼다.
--
--   ② 카운트 게이트가 악재와 역방향 — `news_unique_count>=2` 는 텔레그램 언급 수다.
--      실측(7/29~8/5 라벨 156행): 전체는 58%(91/156)가 uc>=2 를 통과하는데
--      **sentiment<=40 인 행은 37.5%(3/8)만** 통과한다. 악재는 텔레그램에서 안 퍼진다.
--      veto 발동 조건이 "악재일수록 만족하기 어려운 값"을 요구하고 있었다.
--
--   ③ 문턱 30 이 라벨의 실제 하한선 아래 — 7/7~8/5 라벨 200행의 sentiment 분포는
--      20s:1 / 30s:5 / 40s:24 / 50s:70 / 60s:76 / 70s+:24 다. `<=30` 을 만족한 행은
--      **전 기간 단 1건**(8/5 디앤디파마텍 28)이고 그마저 uc=1 이라 탈락했다.
--      nano 는 진짜 악재를 35 근처에 몰아놨다 — 군산조선소 사망사고 35 · 하이브 수퍼톤 청산 35 ·
--      한화솔루션 압수수색 35 · 현대차 파업 35 · 에코프로비엠 유증 차질 40.
--      프롬프트(`news_material_judge` MATERIAL_PROMPT)가 "호재>50 / 악재<50 / 중립=50"만
--      알려주고 30 이 무엇인지 앵커를 주지 않아 생긴 분포다.
--
-- [왜 프롬프트를 안 고치나] 앵커를 넣으면 라벨 분포가 또 바뀌어 sql/58 과 같은 표본 리셋이
-- 한 번 더 필요하다. 모델 교체(오늘) 직후에 정의까지 흔들면 무엇이 원인인지 분리할 수 없다.
-- 문턱을 데이터에 맞추는 쪽이 싸다.
--
-- [문턱 40 의 근거 — luna 실측] 오늘(8/5) 저장된 라벨은 nano 가 만든 값이고, `.env` 교체로
-- 내일부터는 gpt-5.6-luna 다. 그래서 **같은 코퍼스를 luna 로 재판정해 문턱을 골랐다**
-- (읽기 전용 프로브, DB 미기록. 판정 34종목):
--     <=30: 1건 · <=40: 2건 · <=45: 3건 · <=50: 4건  (평균 68.9, 최저 20)
--     디앤디파마텍 20(nano 28) · 한화솔루션 38(nano 35, 검찰 압수수색) — 저점 2건이 실제 악재
--     LG에너지솔루션 45(파업 갈림길) · POSCO홀딩스 47(중립) — 45 부터 중립이 섞인다
--     HD현대중공업 35→**73**: nano 가 '손배소 각하'(승소=호재)를 악재로 찍은 오탐을 luna 가 교정
-- 즉 luna 에서 문턱 40 은 진짜 악재만 집고, 30 은 하루 0~1건이라 판정까지 또 몇 달 걸린다.
-- ⚠️ 근거가 **1거래일 n=34** 다. 표본 속도(≈2건/일 추정)가 5거래일 뒤에도 크게 다르면
--    문턱은 새 rule 로 재등록해 다시 잰다(사전 등록 원칙 — 이 rule 의 predicate 는 안 고친다).
--
-- [왜 카운트 조건을 통째로 빼나] ②가 이 rule 이 재려는 신호와 반대 방향이고, 오늘 luna 저점
-- 2건(디앤디파마텍·한화솔루션)이 **둘 다 uc=1** 이라 카운트 게이트를 남기면 완화 문턱도 0건이다.
-- '복수 기사 확인'이 하던 노이즈 방어는 이제 코퍼스 선별이 대신한다 — `select_headlines` 가
-- 시세보도를 빼고 `mentions_ticker` 가 스쳐 언급 기사를 걸러 판정 대상을 좁힌다(8/5 전환).
--
-- [왜 candidate 이고 왜 1종인가] live 문턱을 넓히는 건 실탄 경로의 발동 범위 변경이다.
-- 검증된 적 없는 문턱으로 그렇게 하지 않는다(veto-verify-before-live 규율). 그리고 이미
-- candidate 가 33종이라 후보 추가는 공짜가 아니다(다중검정 — edge_policy DISCOVERY_DAYS 주석)
-- → 문턱·카운트를 따로 재는 2종이 아니라 **둘을 합친 1종만** 등록한다. 어느 쪽이 효과인지는
-- 현행 `veto_bad_news`(문턱 30 + 카운트 유지)와의 뺄셈으로 사후 분리한다.
--
-- [판정] veto 게이트라 **제외 대상의 mean_net 이 음수**여야 이득이다(제외했는데 그 종목들이
-- 평균 상승이면 우리가 손해 보고 있다는 뜻). 승격 판정은 core.edge_policy 단일 소스.
-- registered_at 을 내일로 잡는 이유는 sql/58 과 같다 — 오늘 행의 라벨은 구 모델(nano)이 만들었다.
--
-- [되돌리기] DELETE FROM edge_rule WHERE name='veto_bad_news_wide';
--            DELETE d FROM edge_rule_daily d JOIN edge_rule r ON r.id=d.rule_id
--             WHERE r.name='veto_bad_news_wide';   (rule 삭제 전에 실행)

INSERT IGNORE INTO edge_rule
  (name, title, family, role, description, predicate, exit_label, status, min_sample, registered_at)
VALUES
('veto_bad_news_wide', '악재 뉴스 제외 (완화 문턱)', 'f1_news', 'veto',
 'AI가 뉴스를 읽어 악재로 판단한 종목을 매수 대상에서 빼는 규칙입니다. 기존 "악재 뉴스 제외" 규칙과 같은 아이디어지만 조건을 두 군데 고쳤습니다. 첫째, 악재로 보는 기준을 30점 이하에서 40점 이하로 넓혔습니다 — AI가 실제로 매긴 점수를 한 달간 모아보니 사망사고·압수수색·청산 같은 확실한 악재도 35~40점으로 찍혀서, 30점 이하는 한 달에 한 건도 나오지 않았습니다. 둘째, "서로 다른 기사 2건 이상"이라는 조건을 뺐습니다. 이 건수는 텔레그램에 몇 번 언급됐는지를 세는 값인데, 악재는 원래 텔레그램에서 잘 퍼지지 않아서 조건을 만족하기가 오히려 어려웠습니다. 대신 뉴스를 읽을 때 단순 시세 보도와 이 종목과 무관한 기사를 걸러내는 장치가 이미 앞단에 있습니다. 아직 실제 매매에는 쓰지 않고 성적만 기록합니다 — 제외한 종목들이 다음 날 평균적으로 떨어져야 이 규칙이 이득입니다.',
 '[{"col":"news_sentiment","op":"<=","value":40}]',
 'exec_leg_ret', 'candidate', 20, CURDATE() + INTERVAL 1 DAY);

-- 확인
SELECT name, role, status, predicate, min_sample, registered_at
  FROM edge_rule WHERE name IN ('veto_bad_news', 'veto_bad_news_wide');
