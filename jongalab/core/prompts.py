"""
AI 프롬프트 템플릿 모듈 - YouTube/Telegram 공통 분석 프롬프트 관리
"""

# 공통 분석 지시사항 (YouTube, Telegram 모두 사용)
_COMMON_ANALYSIS_INSTRUCTIONS = """
[중요 지시사항]
먼저 이 메시지가 **'주식, 경제, 투자, 기업 분석, 시장 전망'**과 관련된 내용인지 판단해.

1. 만약 **관련 없는 내용(일상, 먹방, 게임, 단순 유머 등)**이라면:
   반드시 JSON의 sentiment_score를 **-1**로 설정하고 content는 비워둬.
   
2. **관련 있는 내용**이라면 다음 필드를 분석해서 반드시 **순수 JSON**으로만 출력해.
    - sentiment_score: 시장 전망 점수 (0: 폭락/공포 ~ 50: 중립 ~ 100: 폭등/탐욕). 관련 없으면 -1
    - content: 아래 Markdown 템플릿을 반드시 따르는 분석 리포트 문자열. 관련 없으면 빈 문자열 ""
    - related_companies: 텍스트에서 언급된 상장 기업명 리스트 (최대 5개). 없으면 빈 리스트 []
        🚨주의: 반드시 '현재 주식 시장에 상장된 공식 기업'의 이름만 추출해라. Grok, OpenAI, ChatGPT 같은 제품명, AI 모델, 비상장 기업은 절대 포함하지 마라!
        🚨주의: 기업명만 단독으로 작성해라. 절대로 "엔비디아(NVDA)", "Tesla(TSLA)" 같은 '이름(티커)' 형식을 사용하지 마라! 올바른 예: "엔비디아", "테슬라". 잘못된 예: "엔비디아(NVDA)", "Tesla(TSLA)", "삼성전자(005930)"
        🚨주의: 티커 심볼(NVDA, AAPL, TSLA, 005930 등)을 절대 넣지 마라! 반드시 정식 기업명(엔비디아, 애플, 테슬라, 삼성전자)으로 작성해라.
        🚨주의: "반도체주", "AI주", "2차전지주", "배터리주", "자동차주", "IT주", "바이오주" 같은 섹터/테마/업종 카테고리명은 기업이 아니다. 절대 포함하지 마라! 구체적인 개별 기업명만 넣어라.

    [content Markdown 템플릿 - 반드시 이 구조를 따를 것]:
    
        ## 1. 3줄 핵심 요약
        - (요약 1)
        - (요약 2)  
        - (요약 3)
        
        ## 2. 주요 언급 종목
        - **종목명**: (호재/악재 판단)
        
    [🚨 "## 2. 주요 언급 종목" 작성 절대 규칙]:
    - 반드시 상장된 개별 기업의 **공식 기업명만** 작성해라. (브랜드/제품명/서비스명/인물명/섹터/테마/지수는 금지)
    - **티커 심볼/거래소 코드/종목 코드/괄호 표기**를 절대 쓰지 마라.
        - ✅ 올바른 예: "- **엔비디아**: 호재", "- **삼성전자**: 악재"
        - 🚫 금지 예: "- **엔비디아(NVDA)**: 호재", "- **Tesla(TSLA)**: 호재", "- **삼성전자(005930)**: 호재", "- **005930**: 호재"
    - 종목명은 반드시 굵게 표시된 **종목명** 자리(예: **엔비디아**)에만 단독으로 쓰고, 괄호/티커/부연설명은 붙이지 마라.
    - 텍스트에 기업명이 **명시적으로** 나오지 않으면 이 섹션을 억지로 채우지 말고 "해당없음" 1줄만 작성해라.
        - 예: "- **해당없음**: 종목 언급 없음"
    - 최종 출력 전에, 이 섹션에 알파벳 대문자 티커(예: AAPL, NVDA), 숫자 코드(예: 005930), 점/접미사(예: .KS, .KQ)가 포함되어 있으면 모두 제거하고 기업명만 남겨라.
        
        ## 3. 대응 전략
        > (한 줄 조언)

🚨 환각(Hallucination) 방지 절대 규칙:
- 반드시 **제공된 텍스트에 명시적으로 작성된 내용만** 분석해라. 텍스트에 없는 정보를 절대 추측하거나 지어내지 마라.
- URL만 있고 실제 기사/분석 내용이 없는 텍스트는 분석할 수 없다. sentiment_score를 **-1**로 설정해라.
- sentiment_score는 반드시 텍스트의 실제 논조와 일치해야 한다:
    - "급락", "하락", "폭락", "우려", "경고", "리스크" 등 부정적 키워드가 포함되면 점수는 반드시 50 미만이어야 한다.
    - "급등", "상승", "호조", "기대감", "호재" 등 긍정적 키워드가 포함되면 점수는 50 이상이어야 한다.
- related_companies에는 텍스트에서 **직접 언급된 기업만** 포함해라. 유추하거나 연관 기업을 임의로 추가하지 마라.
- title과 content 요약은 반드시 원문 텍스트의 핵심 내용을 반영해야 한다. 원문과 반대되는 방향의 요약은 절대 금지한다.

🚨 출력 형식 절대 규칙:
- 반드시 순수 JSON만 출력해라. 주석(//, /* */)은 절대 사용하지 마라.
- JSON 외의 텍스트, 설명, 인사말은 절대 출력하지 마라.
- content 값은 반드시 위 Markdown 템플릿 형식의 단일 문자열이어야 한다.
"""

_COMMON_JSON_FORMAT = """
[필수 출력 형식 - 순수 JSON만 출력할 것, 주석 금지]:
{{
    "sentiment_score": 75,
    "content": "## 1. 3줄 핵심 요약\\n- 요약1\\n- 요약2\\n- 요약3\\n\\n## 2. 주요 언급 종목\\n- **종목명**: 호재/악재 판단\\n\\n## 3. 대응 전략\\n> 한 줄 조언",
    "related_companies": ["삼성전자", "SK하이닉스"]
}}
"""

# YouTube 분석용 프롬프트 (title + content 플레이스홀더)
YOUTUBE_ANALYSIS_PROMPT = """
        영상 제목: {title}
""" + _COMMON_ANALYSIS_INSTRUCTIONS + _COMMON_JSON_FORMAT + """
        [자막 내용]: {content}
"""

# Telegram 분석용 프롬프트 (title 자동 생성 포함)
TELEGRAM_ANALYSIS_PROMPT = _COMMON_ANALYSIS_INSTRUCTIONS + """
    - title: 메시지의 핵심 내용을 한눈에 파악할 수 있는 제목 (🚨 아래 title 작성 규칙 반드시 준수)

    [🚨 title 작성 규칙 - 절대 빈 문자열 금지]:
    - title은 **필수 항목**이다. 절대로 빈 문자열("")이나 "제목", "없음" 같은 의미 없는 값을 넣지 마라.
    - 반드시 메시지의 **핵심 주제, 종목명, 시장 방향성**을 포함한 구체적인 제목을 작성해라.
    - 15~40자 사이의 한국어 제목을 작성해라.
    - 뉴스 헤드라인처럼 간결하고 핵심을 담아라.
    - 좋은 예시: "엔비디아 실적 발표 후 시간외 급등", "미국 CPI 예상 하회로 금리 인하 기대감 확대", "테슬라 중국 판매량 급감 우려"
    - 나쁜 예시: "", "제목", "분석", "텔레그램 메시지", "주식 관련"
""" + """
[필수 출력 형식 - 순수 JSON만 출력할 것, 주석 금지]:
{{
    "sentiment_score": 75,
    "title": "엔비디아 실적 호조에 AI 반도체주 동반 상승 전망",
    "content": "## 1. 3줄 핵심 요약\\n- 요약1\\n- 요약2\\n- 요약3\\n\\n## 2. 주요 언급 종목\\n- **종목명**: 호재/악재 판단\\n\\n## 3. 대응 전략\\n> 한 줄 조언",
    "related_companies": ["삼성전자", "SK하이닉스"]
}}

[메시지 내용]: {text}
"""

# 주간 가중치 튜닝 프롬프트 (current_weights + dataset_table 플레이스홀더)
WEIGHT_TUNING_PROMPT = """당신은 한국 주식 종가베팅 전략의 퀀트 분석가입니다.
종가베팅은 '당일 종가에 매수 → 익일 아침에 매도'하는 단기 전략이며, 매수 후보는 아래 지표들을
가중합한 '종합점수'로 선정됩니다. 지난 한 주간 실제 매매된 종목들의 종합점수 지표와 실현손익을
분석해, 오른 종목(WIN)과 떨어진 종목(LOSS)을 가른 지표가 무엇인지 파악하고 가중치를 세부 조정하세요.

[현재 종합점수 구성 가중치]
{current_weights}

가중치 의미 (모든 값은 가점이며 소수 첫째자리까지 가능. 최종 종합점수는 가점들의 최대 합으로 100점 만점 환산됨):
- SCORE_SUPPLY_BONUS: 5일 수급점수(0~100) 비율만큼 부여되는 최대 가점 (종합점수 기여의 핵심)
- SCORE_MA_ALIGNED_BONUS: 이동평균 정배열 가점
- SCORE_NEAR_HIGH_BONUS: 신고가 근처 가점
- SCORE_PREFERRED_VALUE_BONUS / SCORE_MIN_VALUE_BONUS: 거래대금 상위/최소 기준 충족 가점
- SCORE_LEADER_BONUS: 섹터 대장주 가점
- SCORE_EXTRA_SUPPLY_DAY_BONUS: 5일 초과 장기 연속 수급 1일당 가점
- SCORE_PROGRAM_BUY_BONUS: 프로그램 순매수(양매수, prog_net_buy>0) 가점 — 과거 데이터는 프로그램 기능 도입 전이라 N으로 기록될 수 있으니, 표본이 불충분하면 현재값을 유지하세요
- THEME_STOCK_BONUS: 오늘의 테마주 가점
- CONTENT_SCORE_MAX: 콘텐츠(유튜브/텔레그램) 분석 가점 상한
- SCORE_NEWS_BONUS: 뉴스 속보 재료 가점 상한 (종목별 '뉴스언급' 건수가 많을수록 부여). 신규 지표로 기본값 0에서 시작

[지난 주 매매 결과 — 종목별]
{dataset_table}

[조정 규칙]
1. WIN 종목에서 높고 LOSS 종목에서 낮았던 지표의 가중치는 올리고, 그 반대는 내립니다.
2. 각 가중치는 현재값 대비 최대 ±15% 까지만 조정합니다(표본이 한 주뿐이므로 과감한 변경 금지).
3. SCORE_SUPPLY_BONUS 는 수급 중심 전략의 핵심이므로 10~80 범위를 벗어나지 않습니다.
4. 모든 값은 소수 첫째자리까지만 사용합니다.
5. 어떤 지표도 WIN/LOSS 를 뚜렷하게(일관되게) 가르지 못하거나 표본이 적으면, **모든 지표의 현재값을 그대로 유지하는 것(무변경)이 올바른 답**입니다. 노이즈에서 억지로 패턴을 만들어 조정하지 마세요 — 근거가 약하면 변경하지 않는 편을 우선합니다.
6. SCORE_NEWS_BONUS 는 신규 지표라 현재값이 0에 가깝습니다. '뉴스언급'이 WIN 종목에서 뚜렷이 높고 LOSS 종목에서 낮았다면 0에서 소폭(최대 3.0)만 올려 도입하고, 근거가 약하면 0을 유지하세요. (±15% 규칙은 0 근처에서 무의미하므로 이 지표에만 절대 스텝을 허용합니다.)

반드시 아래 JSON 형식으로만 답하세요. weights 에는 위 11개 키를 모두 포함하고, rationale 에는
어떤 지표가 승패를 갈랐고 왜 그렇게 조정했는지 2~4문장으로 한국어로 설명하세요.
{{
    "weights": {{
        "SCORE_SUPPLY_BONUS": 40,
        "SCORE_MA_ALIGNED_BONUS": 10,
        "SCORE_NEAR_HIGH_BONUS": 10,
        "SCORE_PREFERRED_VALUE_BONUS": 15,
        "SCORE_MIN_VALUE_BONUS": 8,
        "SCORE_LEADER_BONUS": 10,
        "SCORE_EXTRA_SUPPLY_DAY_BONUS": 3,
        "SCORE_PROGRAM_BUY_BONUS": 10,
        "THEME_STOCK_BONUS": 15,
        "CONTENT_SCORE_MAX": 10,
        "SCORE_NEWS_BONUS": 0
    }},
    "rationale": "..."
}}
"""
