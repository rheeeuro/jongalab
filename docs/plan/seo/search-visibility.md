# 검색 유입 확보 계획 (2026-08-14 작성)

> 이 문서는 **앞으로 무엇을 할지**를 담는다. 현재 코드 상태는 `jongalab/frontend/README.md`,
> 실행 후의 판정·수치는 `docs/history/` 소관이다.

## 1. 점검으로 확인한 사실

2026-08-14 Search Console 지표와 라이브 사이트 크롤로 확인한 내용이다.

### 사이트 전체
| 지표 | 값 |
|---|---|
| 평균 CTR | 0.5% |
| 평균 게재순위 | 19.7 |

### 경로별
| 경로 | 페이지 수 | 노출 | 페이지당 노출 | 평균순위 | 클릭 |
|---|---|---|---|---|---|
| `/stocks/` | 2,921 | 157 | 0.05 | 44.4 | 0 |
| `/reports/` | 961 | 251 | 0.26 | 7.0 | 0 |

두 경로 가중평균 순위가 21.4인데 사이트 전체가 19.7이므로, 나머지(홈·`/record`·`/news`·
`/market`·`/lab`)가 더 나은 순위에서 클릭 전량을 담당한다. 전체 CTR 0.5%를 역산하면
총 노출 500~650회에 **클릭 2~3회** 수준이다.

### `/reports/` 노출을 만든 실제 검색어
| 검색어 | 노출 |
|---|---|
| 삼성전기 009150 2026년 8월 7일 종가 | 2 |
| 현대차 005380 2026년 8월 7일 종가 | 2 |
| 삼성전자 8월 11일 2026 종가 외국인 순매수 | 1 |
| sk하이닉스 2026-07-09 종가 | 1 |
| 현대차 005380 2026-08-07 종가 | 1 |
| 삼성전자 2026년 8월 7일 종가 주가 | 1 |
| 인선이엔티 | 1 |
| 한탑 | 1 |

노출 251회 중 목록에 드러난 건 10회뿐이다. 나머지 241회는 GSC가 익명 처리한 극단적 롱테일 —
검색어 하나당 노출 1회짜리 파편이라 **반복 검색되는 쿼리를 하나도 잡지 못하고 있다**.

## 2. 진단

세 가지를 차례로 확인해 배제했다.

1. **CTR 0.5%는 이상치가 아니다.** 게재순위 19.7위의 기대 CTR이 0.5~1%다. 스니펫 문제가 아니다.
2. **얇은 페이지가 노출을 잠식한 것도 아니다.** `/stocks/` 2,921개가 만든 노출은 157회로,
   페이지당 0.05회다. CTR을 끌어내린 게 아니라 **애초에 색인되지 않았다**.
3. **실제 원인은 의도 불일치다.** 리포트 상세는 평균 7위라 순위는 이미 확보돼 있는데 클릭이 0이다.
   노출을 만든 검색어가 전부 "종목명 + 날짜 + 종가" 형태 — **시세 조회 의도**인데,
   종가랩 페이지는 추천 근거를 담고 있어 답이 되지 않는다. 게다가 그 질문은 구글 금융 카드와
   네이버금융이 SERP 상단에서 즉답하므로 7위까지 내려올 이유가 없다.

왜 그 검색어에만 걸렸는지는 메타태그가 그대로 설명한다. 당시 리포트 상세는
제목이 `{종목명} · {날짜} 리포트`, description이 `수급 A(8.5점) | 종합 85점 | 기관 +120억 |
외인 +80억` 이었다. 제목의 **종목명 + 날짜**, description의 **외인/기관 순매수**가
"삼성전자 8월 11일 2026 종가 외국인 순매수" 같은 쿼리와 정확히 맞물렸다.

**결론: 기술적 SEO 문제가 아니라 검색 수요와 콘텐츠가 만나지 않는 문제다.**
사이트는 종목 데이터를 나열하는데, 종목 데이터 쿼리는 네이버금융이 전부 가져간다.
종가랩이 이길 수 없는 싸움에 서 있다.

## 3. 이미 적용한 조치 (2026-08-14)

손실을 멈추는 작업만 먼저 반영했다. 트래픽을 늘리는 조치는 아니다.

- **`/stocks/` 색인 대상 축소** — `app/sitemap.ts`가 티커 사전 전체(2,921개) 대신
  **리포트 이력이 있는 종목만** 싣는다. 콘텐츠도 리포트도 없는 종목 페이지는
  `generateMetadata`가 `robots: { index: false }`를 준다.
- **리포트 제목·description 재작성** — 날짜를 제목 앞에서 괄호 뒤로 밀고 앞머리에 검색 의도
  키워드("종가베팅 추천", "추천 근거")를 뒀다. description에서 순매수 금액 나열을 걷어내고
  "왜 골랐나"로 바꿨다. 시세 조회 쿼리 매칭을 줄이려는 것이다.
- **당일 리포트 상세를 사이트맵에 포함** — 이전에는 `/reports/{당일}/{종목}` 10개가 사이트맵에
  없었다(갭 미확정 데이터의 캐시 오염을 피하려 당일을 통째로 제외). 종가베팅은 당일 장 마감
  전에 사는 전략이라 **그 페이지는 하루 지나면 검색 가치가 대부분 사라진다.** 당일치만
  `cache: "no-store"` 로 읽어 원래 가드의 의도(Data Cache 오염 방지)를 지키면서 URL은 싣는다.
  당일치 `changeFrequency` 는 `daily` 로 둬 다음날 아침 갭 결과가 채워진 뒤 재크롤될 여지를 남긴다.

## 4. 남은 과제 — 이길 수 있는 쿼리 정하기

여기가 실제 과제이고, **콘텐츠 전략 결정이라 착수 전 방향 합의가 필요하다.**

### 왜 `/record` 인가
종가랩의 유일한 구조적 차별화는 [`/record`](../../../jongalab/frontend/app/record/page.tsx)다 —
**추천 종목의 실제 성적을 검증 가능한 형태로 공개**하는 것. 네이버금융·인베스팅닷컴은
추천을 하지 않으므로 성적을 공개할 수 없고, 추천을 파는 유료 서비스는 성적을 공개할 이유가 없다.
경쟁이 얕고 검색 의도가 명확한 유일한 지점이다.

노려볼 쿼리 후보: `AI 주식 추천 수익률` · `주식 추천 사이트 승률` · `종가베팅 승률` ·
`종가베팅 수익률` · `AI 추천 종목 성적`

### 현재 격차
- `/record`가 사이트맵에 URL **1개**뿐이다. 월별·전략별로 분해된 페이지가 없어 잡을 수 있는
  쿼리 표면이 하나다.
- `/lab` 전략별 페이지가 있으나 사이트맵에 개별 URL이 없고 description도 없다.
- 위 쿼리들의 답이 되려면 **누적 승률·평균 수익률을 문장으로 서술한 텍스트**가 필요한데,
  현재 화면은 숫자와 차트뿐이라 검색엔진이 읽을 본문이 사실상 없다.

### 착수 전 결정할 것
1. 타겟 쿼리를 몇 개로 좁힐 것인가 (1개 집중 vs 3~5개)
2. `/record` 하위에 월별(`/record/2026-08`)·전략별 페이지를 팔 것인가
3. 성적을 서술형 본문으로 낼 것인가 (자동 생성 문장 vs 수기 작성)
4. 검증 기간 — 위 조치 반영 후 색인·순위가 움직이는 데 4~8주가 걸리므로,
   그 전에 효과를 판단하지 않는다

### 착수하지 말아야 할 것
description 보강·JSON-LD·OG 이미지·파비콘 교체 같은 기술적 SEO는 **지금 하지 않는다.**
효과가 측정될 트래픽 자체가 없어 작업량 대비 확인이 불가능하다.
타겟 쿼리에서 페이지 1 노출이 잡히기 시작한 뒤에 한다.

## 5. 참고 — 점검 중 확인했으나 보류한 항목

나중에 4번이 진척된 뒤 다시 볼 것들이다.

| 항목 | 현재 상태 | 보류 이유 |
|---|---|---|
| 구조화 데이터(JSON-LD) | 코드베이스 전체 0건 | 리치 결과는 순위 확보 후 의미 |
| `og:image` | 전 페이지 없음 | 검색 CTR과 무관(공유 썸네일용) |
| 루트 `openGraph` | 미선언 (`/reports/*` 2종만 있음) | 위와 같음 |
| 파비콘 | `logo.png` 2048×2048 / 935KB | 구글 권장은 48의 배수 정사각형. 매 페이지 935KB 전송은 별개로 낭비 |
| 홈 페이지 무게 | HTML 2.17MB (97%가 RSC flight payload), 본문 텍스트 1,607자 | CWV → 순위 경로라 간접적. 별도 과제 |
| `/stocks/` 내부 링크 | 홈에서 5개, 리포트 상세에서 0개 | 색인 대상으로 남긴 종목은 링크가 필요. 4번과 함께 정리 |
